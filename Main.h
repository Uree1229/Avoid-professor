#include <bangtal.h>
extern int gametype;
extern SceneID scene, scene1, scene2, scene3, scene4;
extern ObjectID door[4];

ObjectID createObject(const char* image, SceneID scene, int x, int y, bool shown);

int random_number(int stat, int end);;
